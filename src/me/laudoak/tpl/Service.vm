package ${packageName}.service;

import ${packageName}.model${beanName};

import java.util.List;

/**
 * generated by SQL2Q
 * on ${date}
 */
public interface ${beanName}Service
{
    Integer insert(${beanName} ${paramName});

    Integer delete(${beanName} ${paramName});

    Integer update(${beanName} ${paramName});

    ${beanName} selectOne(Long id);

    List<${beanName}> select();

    Long count();

}
